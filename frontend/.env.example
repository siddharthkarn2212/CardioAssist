# Backend API base URL (no trailing slash, no /api suffix)
# In production: set this to your Render backend URL
# e.g. VITE_API_URL=https://cardioassist-api.onrender.com
# In local dev: leave commented — Vite proxy handles /api → localhost:8000
# VITE_API_URL=https://your-api.onrender.com
