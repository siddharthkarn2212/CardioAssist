Restructure this entire project into a clean production-ready full-stack architecture for deployment.

IMPORTANT:

* DO NOT change UI, styling, or functionality
* DO NOT rewrite components
* ONLY clean and reorganize structure + fix dependencies

GOAL:
Frontend → Vercel
Backend → Render
Code → GitHub (clean and professional)

---

1. CREATE CLEAN STRUCTURE:

/frontend
/backend

---

2. FRONTEND SETUP:

Move ONLY the actual React app into /frontend:

Include:

* src/
* public/
* index.html
* package.json (cleaned)
* vite.config.ts (cleaned)
* tailwind.config.js
* postcss.config.js

REMOVE from frontend:

* artifacts/
* mockup-sandbox/
* replit configs
* pnpm workspace configs
* any "catalog:" dependencies

---

3. FIX FRONTEND package.json:

Replace ALL dependencies with stable npm versions.

Ensure this exact structure:

{
"name": "cardioassist-frontend",
"version": "1.0.0",
"private": true,
"scripts": {
"dev": "vite",
"build": "vite build",
"preview": "vite preview"
},
"dependencies": {
"react": "^18.3.1",
"react-dom": "^18.3.1",
"@tanstack/react-query": "^5.59.0",
"zod": "^3.23.8",
"lucide-react": "^0.468.0",
"class-variance-authority": "^0.7.0",
"tailwind-merge": "^2.5.2",
"react-hook-form": "^7.53.0",
"recharts": "^2.12.7",
"wouter": "^3.3.5"
},
"devDependencies": {
"vite": "^5.4.10",
"@vitejs/plugin-react": "^4.3.1",
"tailwindcss": "^3.4.13",
"postcss": "^8.4.47",
"autoprefixer": "^10.4.20",
"typescript": "^5.6.2"
}
}

---

4. FIX vite.config.ts:

Remove ALL Replit plugins.

Use ONLY:

import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
plugins: [react()],
});

---

5. FIX TAILWIND:

index.css MUST be:

@tailwind base;
@tailwind components;
@tailwind utilities;

---

6. BACKEND SETUP:

Move backend (API / ML / server code) into /backend

Ensure:

* package.json exists
* server runs with: npm start

Example package.json:

{
"name": "cardioassist-backend",
"version": "1.0.0",
"main": "index.js",
"scripts": {
"start": "node index.js"
}
}

---

7. REMOVE ALL UNUSED FILES:

Delete:

* artifacts/
* mockup-sandbox/
* api-server (old)
* pnpm-lock.yaml
* replit configs

---

8. VERIFY:

Frontend runs:
npm install
npm run dev
npm run build

Backend runs:
npm install
npm start

---

9. FINAL STRUCTURE MUST BE:

/frontend
/src
/public
package.json
vite.config.ts
tailwind.config.js
postcss.config.js

/backend
server files
package.json

---

10. GITHUB:

Ensure project is ready to push cleanly (no broken dependencies).

---

RETURN:

* Final folder structure
* Confirmation frontend build works
* Confirmation backend runs
